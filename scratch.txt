How can I externalize the configuration for database URIs and NATS subjects?

# Example: Use a different NATS server and MongoDB database
export NATS_URL="nats://my-custom-nats:4222"
export MONGO_DATABASE="production_data"

# Now run the applications
go run publisher.go config.go
go run subscriber.go models.go config.go
go run viewer.go models.go config.go



git config --global user.email ".com"
  git config --global user.name "raghavkgarg"